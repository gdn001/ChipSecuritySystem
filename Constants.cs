namespace ChipSecuritySystem
{
    public static class Constants
    {
        public const string ErrorMessage = "Cannot unlock master panel";
        public const Color StartColor = Color.Blue;
        public const Color EndColor = Color.Green;
    }
}
